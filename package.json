{
  "name": "book-management",
  "version": "1.0.0",
  "description": "Book Management Assignment",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd server && npm run start-server\" \"cd client && npm run start-client\"",
    "install-all" : "npm install && cd server && npm install && cd ../ && cd client && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
